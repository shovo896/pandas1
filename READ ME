# pandas1 

A collection of Python scripts demonstrating custom implementations and usage examples for basic pandas-like operations and DataFrame manipulations.

 Structure :

This repository includes multiple .py files, each showcasing different functionalities such as:

- Reading and writing CSV files (read_csv.py, write_csv.py)
- Basic DataFrame operations:
  - indexing (indexing.iloc.py, select_index_slicing.py)
  - filtering (boolean_index.py, slicing.py)
  - aggregation and group operations (aggregate.py, aggregate_multiple.py,different_agggre.py)
- Data cleaning: duplicate handling, filling missing values (fill_missing values.py, remove_mistaken.py, duplicates_dict.py, duplicated.py)
- Data reshaping: pivot, melt, pivot tables (pivot.py, melt ()freshshape.py, reshape_pivot_table.py, reshape pivot.py)
- Categorical and data type conversion (categorical.py, astype_func. astype_func.)
- Joins and merging (merge.py, merge_dataframes.py)
- Feature implementations: dummies, multi-index, label slicing, etc.

Getting Started

 Prerequisites

- Python 3.x installed
- Optionally `pandas` if testing against real DataFrame structures

unning Examples

Each script can be executed individually. For example:

bash
python3 read_csv.py
python3 aggregate.py
